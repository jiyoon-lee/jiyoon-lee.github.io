데이터 전처리 과정을 배워봅시다.
# 결치값

# 이상치
- 일반적인 값과 편차가 큰 값을 지칭
- 이런 이상치는 **평균, 표준편차에 영향을 크게 미쳐요!**
- 이상치는 독립변수의 이상치(지대점), 종속변수의 이상치(outlier)로 나뉩니다.
  - 일반적으로 종속변수의 이상치를 이상치라고 합ㄴ다.
이상치를 알아내기 위해서는 어떻게 해야할까요.
1. Tukey's Fences(사분위 기반)
2. Z-Score(정규분포)

# 정규화
